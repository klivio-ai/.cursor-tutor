{
  "name": "property-finance-dashboard",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && yarn install && yarn build",
    "start": "cd frontend && yarn start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}