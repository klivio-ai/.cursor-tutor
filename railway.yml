version: 2

build:
  dockerfile: Dockerfile

deploy:
  healthcheckPath: /
  healthcheckTimeout: 30
  restartPolicyType: always

environments:
  production:
    variables:
      PORT: 3000
      NODE_ENV: production
